[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnhandledThreadExceptionWarning
markers =
    slow: slow tests (provider outage drill, load test)
    e2e: end-to-end tests requiring running servers
